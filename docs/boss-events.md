# 周期 Boss 事件

周期 Boss 事件把“什么时候刷、刷在哪里、用什么结构、生成哪个 Boss、给什么奖励”集中到 `boss-events.yml`。

能力：按权重选择事件、活动区域保护、内置结构和原版 `.nbt` 结构。Litematica `.litematic` 需要先转换为原版结构文件。

管理命令：`/rw boss ...`。

活动区域会阻止挖掘、放置、水桶/岩浆桶和爆炸破坏区域内方块。Boss YAML 只描述 Boss 个体和动作；周期、区域、结构、奖励写在 `boss-events.yml`。
